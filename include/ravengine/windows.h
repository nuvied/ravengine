#pragma once

namespace ravengine {

// Creates a simple window and keeps it open until the user closes it.
bool show_basic_window(const char* title = "Ravengine", int width = 960, int height = 540);

} // namespace ravengine
